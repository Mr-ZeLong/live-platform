version: '3'
services:
  live-id-generate-provider-1:
    container_name: live-msg-provider-1
    image: 'live-msg-provider-docker:1.0'
    ports:
      - "9098:9098"
    volumes:
      - /tmp/logs/live-msg-provider:/tmp/logs/live-msg-provider
    environment:
      - DUBBO_IP_TO_REGISTRY=192.168.1.7
      - DUBBO_PORT_TO_REGISTRY=9098
      - TZ=Asia/Shanghai
      - JAVA_OPTS=-XX:MetaspaceSize=128m -XX:MaxMetaspaceSize=128m -Xms512m -Xmx512m -Xmn128m -Xss256k
    extra_hosts:
      - 'live-server:192.168.0.254'